# Example environment variables for the crypto order book project

# Local directory for data (relative or absolute path)
DATA_ROOT=./data

# Google Drive folder ID containing the raw logs and data
DRIVE_FOLDER_ID=enter_your_drive_folder_id_here

# Path to your log parser script (if it lives outside this repo)
LOG_PARSER_PATH=./logparser.py

# Optionally, API credentials for Google Drive (if using pydrive2)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REFRESH_TOKEN=
